# GraphRAG configuration
# The database is created by an external indexer (e.g., swift-scip-indexer)

# Path to repository (for context/reference)
repo_path: .

# Path to external indexer SQLite database
db_path: index.db
